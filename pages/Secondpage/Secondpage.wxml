<view class='container'>
<view class="header">
    <text class="new" bindtap="createItem">地址</text>
    <view class="quick">
      <input class="new-quick" value="{{ todo }}" placeholder="快速添加..." bindinput="inputTodo" bindconfirm="addTodo" auto-focus />
    </view>
     <view class="add">
        <text>修改</text>
      </view>
  </view>
 <view class="header">
    <text class="new" bindtap="createItem">手机号</text>
    <view class="quick">
      <input class="new-quick" value="{{ todo }}" placeholder="默认修改" bindinput="inputTodo" bindconfirm="addTodo" auto-focus />
    </view>
    <view class="add">
        <text>修改</text>
      </view>
  </view>
  <view class="header">
    <text class="new" bindtap="createItem">时间</text>
    <view class="quick">
      <input class="new-quick" value="{{ todo }}" placeholder="快速添加..." bindinput="inputTodo" bindconfirm="addTodo" auto-focus />
    </view>
     <view class="add">
        <text>修改</text>
      </view>
  </view>
  <view class="header">
    <text class="new" bindtap="createItem">备注</text>
    <view class="quick">
      <input class="new-quick" value="{{ todo }}" placeholder="快速添加..." bindinput="inputTodo" bindconfirm="addTodo" auto-focus />
    </view>
     <view class="add">
        <text>修改</text>
      </view>
  </view>
</view>
<button type="primary" plain="true" class='button'>确定发单</button>

<!-- .container {
  display: flex;
  justify-content: space-around;
  padding: 200rpx 0;
  margin-left: 50rpx;
} -->