<view class='container'>
  <view class='firstpage-view' wx:for="{{foodlist}}" wx:key="navItems">
    <navigator url='/pages/Detailinfo/Detailinfo'>
      <image src = "{{item.imagePath}}" class='firstpage-image'></image>
    </navigator>
  </view>
</view>